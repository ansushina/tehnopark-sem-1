#include <iostream>

//using namespace std;

int main()
{
    int n;
    std::cin << n;
    int A[n];
    int B[n];

    int imax = 0, jmax = 0, amax = 0;
    for (int i = 0; i < n; i++)
    {
        if (a[i] > a[amax])
        {
            if (a[i] + b[i] > a[imax] + b[imax])
            {
                s
            }
        }
    }
    std::cout << "Hello World!" << std::endl;
    return 0;
}
